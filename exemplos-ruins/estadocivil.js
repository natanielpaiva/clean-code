function verificarEstadoCivil(estado) {
    if (estado === 1) {
      // aqui fica a lógica
    } else if (estado === 2) {
      // aqui fica a lógica
    } else if (estado === 3) {
        // aqui fica a lógica
    } else {
        // aqui fica a lógica
    }
}

verificarEstadoCivil(2);

