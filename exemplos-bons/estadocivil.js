function verificarEstadoCivil(estado) {
    const ESTADO_SOLTEIRO = 1;
    const ESTADO_CASADO = 2;
    const ESTADO_DIVORCIADO = 3;

    if (estado === ESTADO_SOLTEIRO) {
         // aqui fica a lógica
    } else if (estado === ESTADO_CASADO) {
         // aqui fica a lógica
    } else if (estado === ESTADO_DIVORCIADO) {
         // aqui fica a lógica
    } else {
         // aqui fica a lógica
    }
}

verificarEstadoCivil(2);